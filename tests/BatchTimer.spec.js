describe('Batch Timer', function () {
	
	var BatchTimer;
	
	beforeEach(function () {
		
		BatchTimer = require('./../src/BatchTimer');
	});
	
	it('should be something', function () {
		 
		expect(BatchTimer).not.toBe(null);
	});
});